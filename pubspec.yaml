name: dart_ipc
description: "A cross-platform Inter-Process Communication (IPC) library for Dart that provides efficient communication between processes using native platform mechanisms."
version: 2.0.2
homepage: https://github.com/monkeyWie/dart_ipc

environment:
  sdk: ^3.10.0

dependencies:
  ffi: ^2.1.4
  win32: ^6.3.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.27.0

platforms:
  android:
  ios:
  linux:
  macos:
  windows:
